{
  "name": "securasante-backend",
  "version": "1.0.0",
  "description": "API REST — Organisme de Sécurité Sociale",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node -e \"require('./db/database').initDb().then(()=>require('./db/seed').seed()).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "better-sqlite3": "^11.0.0",
    "nodemailer": "^9.0.1",
    "pg": "^8.22.0",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
